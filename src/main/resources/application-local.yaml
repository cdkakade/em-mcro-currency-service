server:
  port: 9081
  error:
    include-message: ALWAYS
    include-binding-errors: ALWAYS
    include-stacktrace: NEVER
    include-exception: false
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/em_currency_db
    username: em_micro_db_user
    password: Lastyear1$
  jpa:
    show-sql: true
